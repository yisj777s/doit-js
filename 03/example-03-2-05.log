let score = prompt("프로그래밍 점수 : ");

if(score != null) { // 값을 입력했을 경우에만 실행합니다.
    if(parseInt(score) >= 90) {
        alert("A 학점");
    } else if(parseInt(score) >= 80) {
        alert("B 학점");
    } else {
        alert("C 학점");
    }
}
undefined
